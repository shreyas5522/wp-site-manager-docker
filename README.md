# wp-site-manager-docker
A command-line script in Bash for managing WordPress sites using Docker containers in a LEMP stack (Linux, Nginx, MariaDB, PHP). It allows easy creation, management (enable/disable), and deletion of WordPress sites on your local development environment.
